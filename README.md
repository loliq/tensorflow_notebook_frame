# tensorflow_notebook_frame
This project is used to do image classification, and it is based on tensorflow1.14(python3.6).
And it frequently uses the slim module, which belongs to tensorflow.contrib module,which was removed  in tensorflow 2.0.
